{
  "templateVersion":  "1.0.1",
  "minSdkVersion":    "9.1.0",
  "minApiLevel":      "4.2.0",
  "targetDeviceId":   "vivoactive6",
  "fallbackDeviceId": "venu3",
  "notes": "minSdkVersion is the minimum Connect IQ SDK installation required to compile. minApiLevel is the API level declared in manifest.xml — these are different fields with different values. Both must match CLAUDE.md sdkconstraints. targetDeviceId is vivoactive6; if absent from installed SDK, fallbackDeviceId venu3 is used.",
  "updateProtocol": "When bumping SDK support: (1) update manifest.xml minSdkVersion, (2) update all four version fields here, (3) run npm run generate-constants:force, (4) run npm test, (5) bump package.json minor version.",
  "updatedAt":        "2026-05-31"
}
